#ifndef STREAM_H
#define STREAM_H

/* Keep this in sync with src/threading/pthread/stream.h. */
#define SNET_STREAM_DEFAULT_CAPACITY  16

typedef struct snet_stream_t snet_stream_t;

#endif /* STREAM_H */
